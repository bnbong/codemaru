# Runtime dependencies for the Vercel Python build.
# Local development uses uv with pyproject.toml; this mirrors the runtime deps
# so Vercel's @vercel/python builder installs them.
fastapi>=0.115.0
pydantic>=2.9.0
pydantic-settings>=2.5.0
jinja2>=3.1.4
httpx>=0.27.0
curl_cffi>=0.7.0
fonttools>=4.50.0
